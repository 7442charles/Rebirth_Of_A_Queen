<%- include('../partials/header') %>

<div class="flex min-h-screen bg-gray-50">
    <%- include('../partials/admin-sidebar', { activePage: 'programs' }) %>

    <main class="flex-1 p-4 md:p-8">
        <div class="max-w-5xl mx-auto">
            <h1 class="text-2xl md:text-3xl font-bold text-[#2D1D30] mb-6 md:mb-8">Programs Manager</h1>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8">
                <!-- Editor -->
                <section class="lg:col-span-1 bg-white p-5 md:p-6 rounded-3xl shadow-sm border border-gray-100">
                    <h2 class="text-lg font-bold mb-4 text-[#2D1D30]">Create / Edit Program</h2>

                    <form id="programForm" class="space-y-4">
                        <input type="hidden" id="programId" name="id" />

                        <div>
                            <label class="block text-sm font-bold text-gray-700 mb-2">Title</label>
                            <input
                                type="text"
                                id="programTitle"
                                name="title"
                                class="w-full p-3 border rounded-xl outline-none focus:border-[#522D59]"
                                placeholder="e.g. Tailoring Training"
                                required
                            />
                        </div>

                        <div>
                            <label class="block text-sm font-bold text-gray-700 mb-2">Body</label>
                            <textarea
                                id="summernotePrograms"
                                name="body_text"
                                class="w-full"
                                placeholder="Write program content..."
                            ></textarea>
                        </div>

                        <button
                            type="submit"
                            class="w-full bg-[#522D59] text-white py-3 rounded-xl font-bold hover:bg-[#3D2142] transition"
                        >
                            Save Program
                        </button>
                    </form>

                    <p id="programStatus" class="mt-3 text-sm text-gray-600"></p>
                </section>

                <!-- List -->
                <section class="lg:col-span-2">
                    <div class="bg-white p-5 md:p-6 rounded-3xl shadow-sm border border-gray-100">
                        <div class="flex items-center justify-between mb-4">
                            <h2 class="text-lg font-bold text-[#2D1D30]">Existing Programs</h2>
                            <span id="programCount" class="text-sm text-gray-500"></span>
                        </div>

                        <div class="space-y-3" id="programList">
                            <% if (programs && programs.length > 0) { %>
                                <% programs.forEach(p => { %>
                                    <button
                                        type="button"
                                        class="w-full text-left p-4 rounded-2xl border border-gray-100 hover:border-[#522D59] transition bg-gray-50"
                                        data-id="<%= p.id %>"
                                        data-title="<%- p.title %>"
                                        data-body="<%- p.body_text || '' %>"
                                    >
                                        <div class="font-bold text-[#2D1D30]"><%= p.title %></div>
                                        <div class="text-sm text-gray-500 mt-1 line-clamp-2"><%- (p.body_text || '').slice(0, 140) %><% if ((p.body_text || '').length > 140) { %>...<% } %></div>
                                    </button>
                                <% }) %>
                            <% } else { %>
                                <div class="py-10 text-center text-gray-400">No programs found.</div>
                            <% } %>
                        </div>
                    </div>
                </section>
            </div>
        </div>
    </main>
</div>

<script>
    $(document).ready(function() {
        $('#summernotePrograms').summernote({
            placeholder: 'Write program content...',
            tabsize: 2,
            height: 260,
            toolbar: [
                ['style', ['style']],

