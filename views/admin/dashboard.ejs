<%- include('../partials/header') %>

<div class="flex min-h-screen bg-gray-50">
    <%- include('../partials/admin-sidebar', { activePage }) %>

    <!-- Main Content -->
    <main class="flex-1 p-4 md:p-8">
        <h1 class="text-3xl font-bold text-[#2D1D30] mb-8">Dashboard</h1>
        
        <div class="bg-white p-6 md:p-8 rounded-3xl shadow-sm border border-gray-100">
            <h2 class="text-xl font-bold mb-6 text-[#2D1D30]">Create New Content</h2>
            <form action="/admin/save" method="POST">
                <input type="text" name="title" placeholder="Post Title" class="w-full p-4 border border-gray-300 mb-6 rounded-xl outline-none focus:border-[#522D59]">
                
                <textarea id="summernote" name="content"></textarea>
                
                <button type="submit" class="mt-6 md:mt-8 bg-[#522D59] text-white px-8 py-3 rounded-xl font-bold hover:bg-[#3D2142] transition">Save Content</button>
            </form>
        </div>
    </main>
</div>

<script>
    $(document).ready(function() {
        $('#summernote').summernote({
            placeholder: 'Start writing your amazing content...',
            tabsize: 2,
            height: 300,
            toolbar: [
                ['style', ['style']],
                ['font', ['bold', 'underline', 'clear']],
                ['color', ['color']],
                ['para', ['ul', 'ol', 'paragraph']],
                ['insert', ['link', 'picture', 'video']]
            ]
        });
    });
</script>
<%- include('../partials/footer') %>
